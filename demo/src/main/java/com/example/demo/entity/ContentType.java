package com.example.demo.entity;

/**
 * Enum representing the type of content a course uses.
 * TEXT - Course content is stored as text in the database
 * PDF - Course content is a PDF file
 */
public enum ContentType {
    TEXT,
    PDF
}
